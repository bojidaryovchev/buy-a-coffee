/**
 * Test stub for the `server-only` package.
 *
 * The real module throws on import so a server module can never be pulled into
 * a client bundle. That protection stays in the application build; this empty
 * stand-in exists purely so unit tests can import the modules it guards.
 */
export {};
